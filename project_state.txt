.
├── README.md
├── assets
│   └── Ahmed_Gomaa_EN.pdf
├── components
│   ├── about.js
│   ├── case-study.js
│   ├── dashboard.js
│   ├── hero.js
│   ├── navigation.js
│   └── projects.js
├── css
│   ├── base.css
│   ├── components.css
│   ├── dashboards.css
│   ├── layout.css
│   ├── responsive.css
│   └── tokens.css
├── data
│   ├── hr.js
│   ├── omnichannel.js
│   └── rfm.js
├── index.html
├── js
│   ├── animations.js
│   ├── app.js
│   ├── charts.js
│   ├── data.js
│   ├── interactions.js
│   └── projects.js
├── project_state.txt
└── projects
    ├── hr.js
    ├── omnichannel.js
    └── rfm.js

7 directories, 28 files


====================
File: ./css/tokens.css
====================
:root {
  --color-bg: #F5F0E8;
  --color-surface: #FBF8F2;
  --color-surface-alt: #EEE7DA;

  --color-ink: #25221E;
  --color-muted: #756E64;
  --color-line: #DCD3C5;

  --color-accent: #8A7650;
  --color-accent-dark: #554A36;

  --color-success: #667A63;
  --color-danger: #A86452;

  --font-display: Georgia, "Times New Roman", serif;
  --font-body: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;

  --text-xs: 0.72rem;
  --text-sm: 0.86rem;
  --text-base: 1rem;
  --text-lg: 1.15rem;
  --text-xl: 1.5rem;
  --text-2xl: 2.25rem;
  --text-3xl: 3.5rem;
  --text-4xl: 5.5rem;

  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-5: 1.5rem;
  --space-6: 2rem;
  --space-8: 3rem;
  --space-10: 4rem;
  --space-12: 6rem;
  --space-16: 8rem;

  --radius-sm: 0.35rem;
  --radius-md: 0.65rem;
  --radius-lg: 1rem;

  --container: 1240px;

  --ease-out: cubic-bezier(0.22, 1, 0.36, 1);
  --duration-fast: 180ms;
  --duration-base: 420ms;
  --duration-slow: 800ms;
}


====================
File: ./css/base.css
====================
*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  background: var(--color-bg);
  color: var(--color-ink);
  font-family: var(--font-body);
  font-size: var(--text-base);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}

body.is-locked {
  overflow: hidden;
}

img,
svg,
canvas {
  display: block;
  max-width: 100%;
}

button,
input,
textarea,
select {
  font: inherit;
}

button {
  cursor: pointer;
}

a {
  color: inherit;
  text-decoration: none;
}

::selection {
  background: var(--color-ink);
  color: var(--color-bg);
}

:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 4px;
}

h1,
h2,
h3,
h4,
p {
  margin-top: 0;
}

h1,
h2,
h3,
h4 {
  font-family: var(--font-display);
  font-weight: 400;
  line-height: 1.05;
  letter-spacing: -0.025em;
}

p {
  margin-bottom: 1rem;
}

ul,
ol {
  margin: 0;
  padding: 0;
  list-style: none;
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }

  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}


====================
File: ./css/layout.css
====================
.container {
  width: min(calc(100% - 3rem), var(--container));
  margin-inline: auto;
}

.section {
  position: relative;
  padding-block: var(--space-16);
}

.section__header {
  display: grid;
  grid-template-columns: minmax(180px, 0.35fr) minmax(0, 1fr);
  gap: var(--space-8);
  margin-bottom: var(--space-10);
}

.section__eyebrow {
  color: var(--color-muted);
  font-size: var(--text-xs);
  font-weight: 700;
  letter-spacing: 0.14em;
  text-transform: uppercase;
}

.section__title {
  max-width: 900px;
  margin-bottom: 0;
  font-size: clamp(2.4rem, 5vw, var(--text-4xl));
}

.grid {
  display: grid;
  gap: var(--space-5);
}

.grid--2 {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.grid--3 {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.grid--4 {
  grid-template-columns: repeat(4, minmax(0, 1fr));
}

.stack {
  display: flex;
  flex-direction: column;
}

.stack--sm {
  gap: var(--space-3);
}

.stack--md {
  gap: var(--space-5);
}

.stack--lg {
  gap: var(--space-8);
}

@media (max-width: 800px) {
  .container {
    width: min(calc(100% - 2rem), var(--container));
  }

  .section {
    padding-block: var(--space-12);
  }

  .section__header {
    grid-template-columns: 1fr;
    gap: var(--space-4);
    margin-bottom: var(--space-8);
  }

  .grid--2,
  .grid--3,
  .grid--4 {
    grid-template-columns: 1fr;
  }
}


====================
File: ./css/components.css
====================
.site-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 3rem;
  padding: 0.75rem 1.15rem;
  border: 1px solid var(--color-ink);
  border-radius: var(--radius-sm);
  background: var(--color-ink);
  color: var(--color-bg);
  font-size: var(--text-sm);
  font-weight: 700;
  transition:
    transform var(--duration-fast) var(--ease-out),
    background var(--duration-fast) ease,
    color var(--duration-fast) ease;
}

.site-button:hover {
  transform: translateY(-2px);
  background: var(--color-accent-dark);
}

.site-button--secondary {
  background: transparent;
  color: var(--color-ink);
}

.site-button--secondary:hover {
  background: var(--color-surface);
  color: var(--color-ink);
}

.eyebrow {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  color: var(--color-muted);
  font-size: var(--text-xs);
  font-weight: 700;
  letter-spacing: 0.14em;
  text-transform: uppercase;
}

.eyebrow::before {
  content: "";
  width: 1.75rem;
  height: 1px;
  background: currentColor;
}

.card {
  border: 1px solid var(--color-line);
  border-radius: var(--radius-md);
  background: var(--color-surface);
}

.kpi-card {
  padding: var(--space-5);
}

.kpi-card__label {
  margin-bottom: var(--space-3);
  color: var(--color-muted);
  font-size: var(--text-xs);
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.kpi-card__value {
  margin: 0;
  font-family: var(--font-display);
  font-size: clamp(2rem, 4vw, 3.25rem);
  font-weight: 400;
  line-height: 0.95;
  letter-spacing: -0.035em;
}

.kpi-card__detail {
  margin: var(--space-3) 0 0;
  color: var(--color-muted);
  font-size: var(--text-sm);
}

.text-link {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  width: fit-content;
  border-bottom: 1px solid currentColor;
  padding-bottom: 0.15rem;
  font-size: var(--text-sm);
  font-weight: 700;
  transition:
    gap var(--duration-fast) var(--ease-out),
    opacity var(--duration-fast) ease;
}

.text-link:hover {
  gap: var(--space-3);
  opacity: 0.72;
}

.tag-list {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-2);
}

.tag {
  display: inline-flex;
  align-items: center;
  min-height: 1.8rem;
  padding: 0.25rem 0.65rem;
  border: 1px solid var(--color-line);
  border-radius: 999px;
  color: var(--color-muted);
  font-size: var(--text-xs);
  font-weight: 700;
  letter-spacing: 0.04em;
}

@media (prefers-reduced-motion: reduce) {
  .site-button:hover,
  .text-link:hover {
    transform: none;
  }
}

.site-nav {
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: center;
  gap: var(--space-6);
  min-height: 5.5rem;
  border-bottom: 1px solid var(--color-line);
}

.site-nav__brand {
  font-family: var(--font-display);
  font-size: 1.35rem;
  line-height: 1;
  letter-spacing: -0.04em;
}

.site-nav__brand span {
  color: var(--color-accent);
}

.site-nav__links {
  display: flex;
  justify-content: center;
  gap: var(--space-6);
}

.site-nav__links a {
  position: relative;
  color: var(--color-muted);
  font-size: var(--text-sm);
  font-weight: 600;
  transition: color var(--duration-fast) ease;
}

.site-nav__links a::after {
  content: "";
  position: absolute;
  right: 0;
  bottom: -0.35rem;
  left: 0;
  height: 1px;
  background: var(--color-ink);
  transform: scaleX(0);
  transform-origin: right;
  transition: transform var(--duration-base) var(--ease-out);
}

.site-nav__links a:hover,
.site-nav__links a[aria-current="page"] {
  color: var(--color-ink);
}

.site-nav__links a:hover::after,
.site-nav__links a[aria-current="page"]::after {
  transform: scaleX(1);
  transform-origin: left;
}

.site-nav__cta {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 2.6rem;
  padding-inline: 1rem;
  border: 1px solid var(--color-line);
  border-radius: var(--radius-sm);
  background: var(--color-surface);
  font-size: var(--text-sm);
  font-weight: 700;
  transition:
    border-color var(--duration-fast) ease,
    transform var(--duration-fast) var(--ease-out);
}

.site-nav__cta:hover {
  border-color: var(--color-ink);
  transform: translateY(-1px);
}

@media (max-width: 800px) {
  .site-nav {
    grid-template-columns: auto auto;
    justify-content: space-between;
  }

  .site-nav__links {
    display: none;
  }
}

@media (prefers-reduced-motion: reduce) {
  .site-nav__links a::after,
  .site-nav__cta {
    transition: none;
  }

  .site-nav__cta:hover {
    transform: none;
  }
}

.hero {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(220px, 0.3fr);
  gap: var(--space-10);
  min-height: calc(100vh - 5.5rem);
  align-items: center;
  padding-block: var(--space-12);
}

.hero__intro {
  max-width: 920px;
}

.hero__title {
  max-width: 950px;
  margin: var(--space-5) 0 var(--space-6);
  font-size: clamp(3.4rem, 8vw, 7.5rem);
  letter-spacing: -0.055em;
}

.hero__title em {
  display: block;
  color: var(--color-accent);
  font-style: italic;
}

.hero__description {
  max-width: 650px;
  margin-bottom: var(--space-8);
  color: var(--color-muted);
  font-size: clamp(1rem, 1.5vw, 1.2rem);
}

.hero__actions {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-3);
}

.hero__aside {
  align-self: end;
  max-width: 240px;
  padding-bottom: var(--space-6);
  border-top: 1px solid var(--color-line);
  color: var(--color-muted);
}

.hero__aside p {
  margin: var(--space-4) 0 0;
  font-family: var(--font-display);
  font-size: var(--text-lg);
  line-height: 1.3;
}

.hero__index {
  display: block;
  padding-top: var(--space-3);
  font-size: var(--text-xs);
  font-weight: 700;
  letter-spacing: 0.1em;
}

@media (max-width: 800px) {
  .hero {
    grid-template-columns: 1fr;
    min-height: auto;
    gap: var(--space-8);
  }

  .hero__aside {
    max-width: 100%;
  }
}

.site-nav__mobile {
  display: none;
}

.site-nav__mobile details {
  position: relative;
}

.site-nav__mobile summary {
  list-style: none;
  cursor: pointer;
  min-height: 2.6rem;
  padding: 0.65rem 0.9rem;
  border: 1px solid var(--color-line);
  border-radius: var(--radius-sm);
  background: var(--color-surface);
  font-size: var(--text-sm);
  font-weight: 700;
}

.site-nav__mobile summary::-webkit-details-marker {
  display: none;
}

.site-nav__mobile-links {
  position: absolute;
  top: calc(100% + 0.6rem);
  right: 0;
  z-index: 20;
  display: grid;
  min-width: 180px;
  padding: 0.45rem;
  border: 1px solid var(--color-line);
  border-radius: var(--radius-md);
  background: var(--color-surface);
  box-shadow: 0 16px 40px rgba(37, 34, 30, 0.10);
}

.site-nav__mobile-links a {
  padding: 0.75rem 0.8rem;
  border-radius: var(--radius-sm);
  color: var(--color-muted);
}

.site-nav__mobile-links a:hover {
  background: var(--color-surface-alt);
  color: var(--color-ink);
}

@media (max-width: 800px) {
  .site-nav__mobile {
    display: block;
    justify-self: end;
    margin-right: 0.5rem;
  }

  .site-nav {
    grid-template-columns: auto 1fr auto;
  }
}

@media (max-width: 520px) {
  .site-nav__cta {
    display: none;
  }

  .site-nav__mobile {
    margin-right: 0;
  }
}


====================
File: ./css/dashboards.css
====================
.dashboard {
  display: grid;
  gap: var(--space-5);
}

.dashboard__toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-4);
  padding: var(--space-4);
  border: 1px solid var(--color-line);
  border-radius: var(--radius-md);
  background: var(--color-surface);
}

.dashboard__filters {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-3);
}

.dashboard__filter {
  display: grid;
  gap: 0.3rem;
}

.dashboard__filter label {
  color: var(--color-muted);
  font-size: var(--text-xs);
  font-weight: 700;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.dashboard__filter select,
.dashboard__filter input {
  min-height: 2.6rem;
  border: 1px solid var(--color-line);
  border-radius: var(--radius-sm);
  background: var(--color-bg);
  color: var(--color-ink);
  padding: 0.45rem 0.7rem;
}

.dashboard__actions {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.dashboard__kpis {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: var(--space-4);
}

.dashboard__grid {
  display: grid;
  grid-template-columns: repeat(12, minmax(0, 1fr));
  gap: var(--space-4);
}

.dashboard__panel {
  min-width: 0;
  padding: var(--space-5);
  border: 1px solid var(--color-line);
  border-radius: var(--radius-md);
  background: var(--color-surface);
}

.dashboard__panel--wide {
  grid-column: span 8;
}

.dashboard__panel--half {
  grid-column: span 6;
}

.dashboard__panel--third {
  grid-column: span 4;
}

.dashboard__panel-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: var(--space-4);
  margin-bottom: var(--space-5);
}

.dashboard__panel-title {
  margin: 0;
  font-family: var(--font-display);
  font-size: var(--text-xl);
  font-weight: 400;
}

.dashboard__panel-note {
  margin: 0;
  color: var(--color-muted);
  font-size: var(--text-sm);
}

.dashboard__chart {
  position: relative;
  min-height: 280px;
}

.dashboard__tabs {
  display: flex;
  gap: 0;
  overflow-x: auto;
  border-bottom: 1px solid var(--color-line);
}

.dashboard__tab {
  flex: 0 0 auto;
  border: 0;
  border-bottom: 2px solid transparent;
  background: transparent;
  color: var(--color-muted);
  padding: 0.75rem 1rem;
  font-size: var(--text-sm);
  font-weight: 700;
}

.dashboard__tab[aria-selected="true"] {
  border-bottom-color: var(--color-ink);
  color: var(--color-ink);
}

.dashboard__status {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  color: var(--color-muted);
  font-size: var(--text-xs);
}

.dashboard__status::before {
  content: "";
  width: 0.45rem;
  height: 0.45rem;
  border-radius: 50%;
  background: currentColor;
}

.dashboard__empty {
  display: grid;
  min-height: 280px;
  place-items: center;
  padding: var(--space-6);
  color: var(--color-muted);
  text-align: center;
}

@media (max-width: 900px) {
  .dashboard__kpis {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .dashboard__panel--wide,
  .dashboard__panel--half,
  .dashboard__panel--third {
    grid-column: span 12;
  }
}

@media (max-width: 600px) {
  .dashboard__toolbar {
    align-items: stretch;
    flex-direction: column;
  }

  .dashboard__filters,
  .dashboard__actions {
    width: 100%;
  }

  .dashboard__filter {
    flex: 1 1 100%;
  }

  .dashboard__kpis {
    grid-template-columns: 1fr;
  }
}


====================
File: ./css/responsive.css
====================
@media (max-width: 1100px) {
  :root {
    --text-4xl: 4.5rem;
    --space-16: 7rem;
  }
}

@media (max-width: 800px) {
  :root {
    --text-3xl: 2.75rem;
    --text-4xl: 3.5rem;
    --space-16: 6rem;
  }
}

@media (max-width: 600px) {
  :root {
    --text-2xl: 2rem;
    --text-3xl: 2.35rem;
    --text-4xl: 3rem;
    --space-12: 4.5rem;
    --space-16: 5rem;
  }

  .container {
    width: min(calc(100% - 1.5rem), var(--container));
  }

  .section {
    padding-block: var(--space-12);
  }
}


====================
File: ./js/app.js
====================
import { renderNavigation } from "../components/navigation.js";
import { renderHero } from "../components/hero.js";
import { renderAbout } from "../components/about.js";

document.addEventListener("DOMContentLoaded", () => {
  renderNavigation();
  renderHero();
  renderAbout();
});


====================
File: ./js/data.js
====================


====================
File: ./js/projects.js
====================


====================
File: ./js/charts.js
====================


====================
File: ./js/animations.js
====================


====================
File: ./js/interactions.js
====================


====================
File: ./data/rfm.js
====================


====================
File: ./data/hr.js
====================


====================
File: ./data/omnichannel.js
====================


====================
File: ./components/navigation.js
====================
export function renderNavigation() {
  const header = document.querySelector("#site-header");

  if (!header) return;

  header.innerHTML = `
    <div class="container">
      <nav class="site-nav" aria-label="Primary navigation">
        <a class="site-nav__brand" href="#hero" aria-label="Ahmed Gomaa home">
          AG<span>.</span>
        </a>

        <div class="site-nav__links">
          <a href="#about">About</a>
          <a href="#services">Services</a>
          <a href="#work">Work</a>
          <a href="#process">Process</a>
          <a href="#contact">Contact</a>
        </div>

        <div class="site-nav__mobile">
          <details>
            <summary aria-label="Open navigation menu">Menu</summary>
            <div class="site-nav__mobile-links">
              <a href="#about">About</a>
              <a href="#services">Services</a>
              <a href="#work">Work</a>
              <a href="#process">Process</a>
              <a href="#contact">Contact</a>
            </div>
          </details>
        </div>

        <a class="site-nav__cta" href="#contact">
          Let's talk
        </a>
      </nav>
    </div>
  `;
}


====================
File: ./components/projects.js
====================


====================
File: ./components/dashboard.js
====================


====================
File: ./components/case-study.js
====================


====================
File: ./components/hero.js
====================
export function renderHero() {
  const hero = document.querySelector("#hero");

  if (!hero) return;

  hero.innerHTML = `
    <div class="container">
      <div class="hero">
        <div class="hero__intro">
          <p class="eyebrow">Data Analyst · SQL · Python · Power BI</p>

          <h1 class="hero__title">
            Turning raw data into
            <em>business decisions.</em>
          </h1>

          <p class="hero__description">
            I clean messy datasets, build the SQL and Python logic behind the numbers,
            and deliver dashboard reports that stakeholders can act on today.
          </p>

          <div class="hero__actions">
            <a class="site-button" href="#work">Explore my work</a>
            <a class="site-button site-button--secondary" href="#contact">Let's talk</a>
          </div>
        </div>

        <div class="hero__aside" aria-label="Portfolio introduction">
          <span class="hero__index">01 / 06</span>
          <p>
            From raw records to structured insight,
            with business context at every step.
          </p>
        </div>
      </div>
    </div>
  `;
}


====================
File: ./components/about.js
====================
export function renderAbout() {
  const section = document.querySelector("#about");

  if (!section) return;

  section.innerHTML = `
    <div class="container">
      <div class="about">
        <div class="section__header about__header">
          <p class="section__eyebrow">02 / About</p>

          <h2 class="section__title">
            Analytical thinking,
            <em>business context.</em>
          </h2>
        </div>

        <div class="about__content">
          <div class="about__statement">
            <p class="about__lead">
              I don't treat analytics as just making charts.
            </p>

            <p>
              I work across SQL, Python, Power BI, and Excel to translate
              operational and transactional data into structured analysis.
              I start from the raw data shape, validate its logic,
              and connect the output directly to a business question.
            </p>
          </div>

          <div class="about__details">
            <p>
              My recent projects include RFM customer segmentation,
              sales performance tracking, and HR workforce analytics —
              focusing on KPIs that managers actually use to evaluate
              retention and revenue.
            </p>

            <div class="about__skills" aria-label="Core tools">
              <span>SQL Server</span>
              <span>Python · Pandas</span>
              <span>Power BI</span>
              <span>Excel · Power Query</span>
            </div>
          </div>
        </div>

        <div class="about__metrics" aria-label="Analytics experience metrics">
          <article class="about__metric">
            <strong>+145K</strong>
            <span>Rows cleaned</span>
          </article>

          <article class="about__metric">
            <strong>+12</strong>
            <span>SQL queries</span>
          </article>

          <article class="about__metric">
            <strong>3</strong>
            <span>Python models</span>
          </article>

          <article class="about__metric">
            <strong>2</strong>
            <span>Dashboards</span>
          </article>
        </div>
      </div>
    </div>
  `;
}


====================
File: ./projects/rfm.js
====================


====================
File: ./projects/hr.js
====================


====================
File: ./projects/omnichannel.js
====================


====================
File: ./index.html
====================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Ahmed Gomaa — Data Analyst Portfolio">
  <title>Ahmed Gomaa — Data Analyst</title>

  <link rel="stylesheet" href="css/tokens.css">
  <link rel="stylesheet" href="css/base.css">
  <link rel="stylesheet" href="css/layout.css">
  <link rel="stylesheet" href="css/components.css">
  <link rel="stylesheet" href="css/dashboards.css">
  <link rel="stylesheet" href="css/responsive.css">
</head>

<body>
  <div id="app">
    <header id="site-header"></header>

    <main>
      <section id="hero"></section>
      <section id="about"></section>
      <section id="services"></section>
      <section id="work"></section>
      <section id="process"></section>
      <section id="contact"></section>
    </main>

    <footer id="site-footer"></footer>
  </div>

  <script type="module" src="js/app.js"></script>
</body>
</html>
